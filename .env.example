# Copy to .env to override the dev defaults in docker-compose.yml.
# These are LOCAL DEV values only. Real secrets are injected at deploy time
# (planned: GitHub Actions / GitHub Environment secrets), never committed.
POSTGRES_PASSWORD=devpassword
JWT_KEY=dev-only-insecure-key-change-me-32+chars!!
SEED_ADMIN_PASSWORD=Admin!Dev12345
